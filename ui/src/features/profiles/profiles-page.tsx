import { StubScreen } from "@/components/stub-screen"

export function ProfilesPage() {
  return (
    <StubScreen title="Profiles" owner="src/features/profiles">
      Agent profiles: role, agent CLI and model, system prompt and extra flags, with create, edit
      and delete.
    </StubScreen>
  )
}
